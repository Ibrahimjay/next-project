/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["fra.cloud.appwrite.io"], // Replace with your Appwrite region if different
  },
};

export default nextConfig;
